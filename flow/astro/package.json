{
  "name": "flow-astro",
  "type": "module",
  "version": "0.0.1",
  "private": true,
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "rm -rf dist && astro build && sed -i '' 's/ data-edit=\"[^\"]*\"//g' dist/*.html dist/*/index.html 2>/dev/null; rm -f dist/edit-mode.js; true",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "astro": "^6.1.6"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.8",
    "typescript": "^5.9.3"
  }
}
